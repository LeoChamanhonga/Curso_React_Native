import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ex: {
        paddingHorizontal: 15,
        marginVertical: 5,
        borderWidth: 5,
        borderColor: '#222',
        fontSize: 24,
        fontWeight: 'bold',
        borderRadius: 10,

    },
    input : {
        height: 70,
        fontSize: 40,
        borderColor: 'gray',
        borderWidth: 1
    }
})
